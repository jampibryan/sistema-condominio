# Componentes Comunes

Aquí se colocarán los componentes UI reutilizables y genéricos de la aplicación:

- Botones (`Button.vue`)
- Tarjetas (`Card.vue`)
- Modales (`Modal.vue`)
- Tablas base (`Table.vue`)
- Elementos de formulario (`Input.vue`, `Select.vue`)
